import unittest
from dataclasses import dataclass


class ConfigTest(unittest.TestCase):
    @unittest.skip("Test is currently a work in progress")
    def test_validate_config(self):
        # TODO; once we get the data model sorted for it better.
        pass
